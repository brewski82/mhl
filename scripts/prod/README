See launch_prod_env.sh for information on how to launch the MHL's application using docker and docker-compose.

Also see https://github.com/wmnnd/nginx-certbot for information on how to use LetsEncrypt with docker nginx images.